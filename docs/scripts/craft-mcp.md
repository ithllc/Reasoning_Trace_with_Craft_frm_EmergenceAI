# `scripts/craft_mcp.py`

Purpose: legacy tenant metadata helper retained for authorized deployments. It fails closed unless the connector and authorization attestation are enabled and tenant endpoint/project variables are present. It must not be used after an authorization expires. Public product documentation access uses the separate documentation MCP. Do not probe undocumented tools, security behavior, or unauthorized data.
